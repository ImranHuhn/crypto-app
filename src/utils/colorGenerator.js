export const randomColor = () => {
    // random color code
}

export const trendColor = (number) => {
    if(number < 0) {
        console.log("negative")
    } else {
        console.log("positive")
    }
}